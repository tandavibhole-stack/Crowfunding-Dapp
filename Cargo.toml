[workspace]
resolver = "2"
members = [
    "contracts/campaign",
    "contracts/factory",
]

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
panic = "abort"
lto = true
codegen-units = 1
